package game;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class PositionTest {

    /*@Test
    void getMove() {
        int[][] rep = new int[3][3];
        for(int i = 0; i < rep.length; i++) {
            for (int j = 0; j < rep[i].length; j++) {
                rep[i][j] = 0;
            }
        }
        Location locationTest = new Location(2, 2);
        Position positionTest = new Position(rep, true, locationTest);

        assertNotEquals(locationTest.getRow(), positionTest.getMove().getRow());
    }

    @Test
    void compareTo() {
        //Position positionTest = new Position();


    }*/

    @Test
    void isFull() {

    }

    @Test
    void eval() {
    }
}